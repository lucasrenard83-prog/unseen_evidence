<div class="sidebar-content">
  <h2>Rooms</h2>
  <hr>
  <div class="sidebar-list">
    <% @rooms.each do |room| %>
      <%= link_to room.name, room_path(room), class: "sidebar-element" %>
    <% end %>
  </div>
</div>
